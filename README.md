# BTC V3.2 自动化交易 · V19.3 刷新事件保护与运行周期最终复检版

本版本以 OKX BTC-USDT-SWAP 的公开实时市场数据为基础进行本地模拟，不连接真实资金。

## 当前参数
- 初始模拟本金：1,000 USDT
- 保证金：全仓（cross）模拟
- 杠杆：10x / 20x，由信号强度自动选择
- 页面加载后自动运行，不需要手动启动
- 人工按钮仅用于“暂停/恢复新开仓”，不会因为暂停而强制平掉已有持仓

## 实时数据
- OKX REST：历史 K 线、ticker、mark price、OI、当前资金费率、合约参数
- OKX WebSocket：ticker、mark-price、open-interest、funding-rate、trades、incremental books、1m/5m/15m/30m/1H K线
- 下单模拟使用实时订单簿吃单：开多吃 ask、开空吃 bid；平多吃 bid、平空吃 ask
- 未实现盈亏使用 OKX mark price
- 资金费在实际结算时间到达后，读取 OKX funding-rate-history 的 realizedRate 模拟结算

## 不盲目开仓
新开仓必须同时通过：
1. 核心行情数据新鲜度检查；
2. 多周期方向评分；
3. 1m/5m/15m 短周期至少两周期同向确认；
4. 主动成交方向确认；
5. OI 短时异常波动过滤；
6. 综合分达到最低开仓阈值。
任何一项不满足，系统继续盯盘和更新数据，但自动暂停新开仓。

## 自主短线退出
持仓后持续重新分析实时行情。系统不会设置固定收益目标，而是根据趋势反转、动能衰减、波动异常、盈利回撤保护等实时条件自主退出。数据异常时优先保护已有持仓，不盲目继续开新单。

## 逐笔订单历史
每一笔订单从开仓开始就创建唯一 Order ID，并在同一条历史记录中持续更新到平仓。记录包括：开仓/平仓时间、方向、开平仓价格、杠杆、全仓模式、保证金、合约数量、名义价值、手续费、资金费、毛/净盈亏、单笔收益率、持仓时长、平仓原因、平仓后权益、最大有利/不利波动。历史订单数量不截断。

## 收益统计
页面实时统计总订单、累计净收益、累计收益率、胜率、盈利/亏损单、最大单笔盈利、最大单笔亏损、最大回撤和当前账户权益。

## 持久化
模拟账户快照同时写入 LocalStorage、IndexedDB 和 Cache Storage，并在页面隐藏/关闭时保存。订单历史不会因普通刷新或重新打开同一浏览器来源而主动清空。清除 Safari 网站数据、使用私密浏览或更换设备/网站来源仍可能导致本地记录不可见；跨设备永久保存需要后端数据库。

## 安全边界
这是浏览器本地模拟交易，不会向 OKX 发送真实下单请求，也不会动用真实资金。以后若接 OKX 官方模拟盘/实盘 API，需要另外实现认证、订单状态回报和服务器端持久化。


本次再次检查修复：初始化 lastTrade=0，避免首次满足条件时因 undefined 导致 Date.now()-lastTrade 为 NaN，从而永远无法自动开第一笔订单。


本版固定初始模拟本金为 1,000 USDT；全仓模式按当前可用现金尽可能使用全部保证金（预留开仓手续费），不再错误限制为历史初始本金。V19 使用独立的持久化命名空间，不导入旧 V14/V16/V17 账户快照，首次启动从干净的 1,000 USDT 模拟账户开始；V19 之后的订单会持续保存在本机并在刷新后恢复。


## V19 复检重点
- 初始模拟账户固定 1,000 USDT；全仓按当前可用现金尽可能使用全部保证金并预留开仓手续费。
- 不读取旧版本的 10,000 USDT 账户存储，避免 Safari 旧数据污染。
- 每个周期 1m/5m/15m/30m/1H 独立输出偏多、偏空或观望。
- 每笔订单保存开仓瞬间的综合分、各周期方向/分数、价格、标记价、OI、资金费率、主动成交比例、OI/价格变化及盘口价格快照。
- Service Worker、JS、IndexedDB、Cache Storage 均升级到 V19 独立命名空间。


## V19 强平价格
- 当前持仓面板显示“预计强平价”“距强平”“强平风险”。
- 这是本地模拟的估算值，使用模拟维持保证金率 0.50% 并计入模拟平仓手续费；实际 OKX 强平价会随账户权益、风险档位、维持保证金率、资金费及其他持仓/订单实时变化。OKX 官方也将 `liqPx` 定义为基于当前权益和保证金率的估算值。
- 模拟系统以 Mark Price 判断是否触及估算强平价；若触及则记录“模拟强制平仓”。


## V19.1 修复
- 持久化保存 `lastTrade` 与 `lastFundingApplied`，刷新 Safari 后不会把交易冷却时间重置为 0。
- Service Worker shell cache 升级为 v2，确保部署更新后不会继续使用旧版 app.js。
- V19 独立账户命名空间保持不变，历史模拟账户不会被迁移或混入旧版本。


## V19.2 修复
- 保留原 V19.1 的 1,000 USDT 模拟本金、全仓 10x/20x、历史订单、账户持久化和现有数据。
- 页面刷新/重新进入后，先恢复状态并完成首轮实时市场分析；首轮分析禁止新开仓，避免刷新本身造成额外交易。
- 每次页面运行生成独立运行周期 ID；新订单记录触发来源与运行周期，便于核对订单是否来自正常自动运行。
- Service Worker shell cache 升级为 v3。

## V19.3 最终复检
- 不改变当前 1,000 USDT 模拟本金、历史订单、账户余额、全仓 10x/20x、OKX 实时数据与动态平仓逻辑。
- 刷新/重新打开只恢复账户并重新连接 OKX；不再使用“30 秒后自动放行”的时间型刷新保护。
- 页面重新建立全部关键 WebSocket 实时数据后，先记录当前信号作为“刷新基线”；只有之后出现新的合格方向/强度变化才允许新开仓。
- REST 历史K线不再直接伪装成“实时新鲜数据”；必须收到对应 WebSocket 实时K线后才计入交易就绪。
- 新订单继续记录触发来源与运行周期 ID。
